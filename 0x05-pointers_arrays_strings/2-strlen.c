#include "main.h"
#include <string.h>

/**
 * _strlen - Returns length of string
 * @s: String input
 *
 * Return: Length of string
 */

int _strlen(char *s)
{
	int length;

	length =  strlen(s);

	return (length);
}
